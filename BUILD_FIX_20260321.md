# GitHub Actions 构建修复说明

## 🔧 修复日期
2026-03-21

## 📋 问题诊断

### 发现的问题

| # | 问题 | 影响 | 状态 |
|---|------|------|------|
| 1 | Gradle 版本不匹配 (7.3.0 → 8.1.0) | 构建失败 | ✅ 已修复 |
| 2 | 缺少 gradle/wrapper 目录 | 无法下载 Gradle | ✅ 已修复 |
| 3 | compileSdkVersion 语法过时 | 警告/兼容性问题 | ✅ 已修复 |
| 4 | targetSdkVersion 过时 (33 → 34) | 安全更新 | ✅ 已修复 |
| 5 | 缺少 namespace 配置 | AGP 8.x 要求 | ✅ 已修复 |

## 🛠️ 修复内容

### 1. 更新 Gradle 版本

**文件**: `android/build.gradle`

```diff
- classpath 'com.android.tools.build:gradle:7.3.0'
+ classpath 'com.android.tools.build:gradle:8.1.0'
```

### 2. 添加 Gradle Wrapper 配置

**文件**: `android/gradle/wrapper/gradle-wrapper.properties` (新建)

```properties
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.1.0-all.zip
```

### 3. 更新 Android 应用配置

**文件**: `android/app/build.gradle`

```diff
android {
+   namespace "com.komga.komga_reader"
-   compileSdkVersion 33
+   compileSdk 34
    ndkVersion flutter.ndkVersion

    defaultConfig {
        applicationId "com.komga.komga_reader"
-       minSdkVersion 21
+       minSdk 21
-       targetSdkVersion 33
+       targetSdk 34
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        multiDexEnabled true
    }
}
```

### 4. 优化 GitHub Actions 工作流

**文件**: `.github/workflows/release.yaml`

添加内容：
- Gradle 缓存步骤
- Flutter Android 预缓存
- 诊断信息输出（flutter doctor, gradle version 等）

## 📊 版本兼容性

| 组件 | 版本 | 说明 |
|------|------|------|
| Flutter | 3.16.0 | 稳定版 |
| Dart | 3.2.0 | 随 Flutter |
| Gradle | 8.1.0 | 兼容 AGP 8.1 |
| AGP | 8.1.0 | Android Gradle Plugin |
| Kotlin | 1.9.0 | 兼容 Gradle 8.x |
| Java | 17 | Gradle 8.x 要求 |
| compileSdk | 34 | Android 14 |
| targetSdk | 34 | Android 14 |
| minSdk | 21 | Android 5.0 |

## ✅ 验证步骤

### 本地验证（如有 Flutter 环境）

```bash
cd komga_reader

# 1. 清理构建
flutter clean

# 2. 获取依赖
flutter pub get

# 3. 运行诊断
flutter doctor -v

# 4. 测试构建
flutter build apk --debug
```

### GitHub Actions 验证

1. 推送代码到仓库
2. 访问 Actions 页面查看构建状态
3. 检查 "诊断信息" 步骤输出
4. 下载构建产物验证

## 🔍 常见错误排查

### 错误 1: Gradle 下载失败

**症状**: `Could not resolve all files for configuration ':classpath'`

**解决**: 
- 检查网络连接
- 确认 gradle-wrapper.properties 中的 URL 可访问
- 尝试使用阿里云镜像

### 错误 2: Java 版本不匹配

**症状**: `Unsupported class file major version`

**解决**:
- 确认使用 Java 17
- 检查工作流中 setup-java 配置

### 错误 3: namespace 未定义

**症状**: `Namespace not specified in build.gradle`

**解决**:
- 在 android {} 块中添加 namespace
- 或使用 android.namespace 在 build.gradle.kts 中

### 错误 4: Flutter 插件找不到

**症状**: `Flutter plugin not found`

**解决**:
- 确保 local.properties 中有 flutter.sdk
- 运行 `flutter precache --android`

## 📝 后续优化建议

1. **添加构建缓存** - 使用 actions/cache 缓存 .gradle 和 .flutter 目录
2. **并行构建** - 使用 matrix 策略同时构建多个版本
3. **自动签名** - 配置 Android 签名密钥
4. **测试覆盖** - 添加单元测试和集成测试
5. **代码质量** - 启用 flutter analyze 严格模式

## 🔗 相关资源

- [Flutter 3.16 发布说明](https://docs.flutter.dev/release/archive)
- [Gradle 8.1 发布说明](https://docs.gradle.org/8.1/release-notes.html)
- [AGP 8.1 迁移指南](https://developer.android.com/build/releases/gradle-plugin)
- [GitHub Actions Flutter](https://github.com/marketplace/actions/flutter-action)

---

**修复完成时间**: 2026-03-21 00:30
**下次检查**: 推送后查看 GitHub Actions 状态
